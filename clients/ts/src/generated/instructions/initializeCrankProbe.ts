/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findInitializeCrankProbeCrankProbePda } from "../pdas";
import { PROTECTED_PAY_PROGRAM_ADDRESS } from "../programs";

export const INITIALIZE_CRANK_PROBE_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([89, 96, 93, 85, 185, 237, 202, 20]);

export function getInitializeCrankProbeDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    INITIALIZE_CRANK_PROBE_DISCRIMINATOR,
  );
}

export type InitializeCrankProbeInstruction<
  TProgram extends string = typeof PROTECTED_PAY_PROGRAM_ADDRESS,
  TAccountPayer extends string | AccountMeta<string> = string,
  TAccountDeposit extends string | AccountMeta<string> = string,
  TAccountCrankProbe extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountPayer extends string
        ? WritableSignerAccount<TAccountPayer> &
            AccountSignerMeta<TAccountPayer>
        : TAccountPayer,
      TAccountDeposit extends string
        ? ReadonlyAccount<TAccountDeposit>
        : TAccountDeposit,
      TAccountCrankProbe extends string
        ? WritableAccount<TAccountCrankProbe>
        : TAccountCrankProbe,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type InitializeCrankProbeInstructionData = {
  discriminator: ReadonlyUint8Array;
  delaySeconds: bigint;
};

export type InitializeCrankProbeInstructionDataArgs = {
  delaySeconds: number | bigint;
};

export function getInitializeCrankProbeInstructionDataEncoder(): FixedSizeEncoder<InitializeCrankProbeInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["delaySeconds", getI64Encoder()],
    ]),
    (value) => ({
      ...value,
      discriminator: INITIALIZE_CRANK_PROBE_DISCRIMINATOR,
    }),
  );
}

export function getInitializeCrankProbeInstructionDataDecoder(): FixedSizeDecoder<InitializeCrankProbeInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["delaySeconds", getI64Decoder()],
  ]);
}

export function getInitializeCrankProbeInstructionDataCodec(): FixedSizeCodec<
  InitializeCrankProbeInstructionDataArgs,
  InitializeCrankProbeInstructionData
> {
  return combineCodec(
    getInitializeCrankProbeInstructionDataEncoder(),
    getInitializeCrankProbeInstructionDataDecoder(),
  );
}

export type InitializeCrankProbeAsyncInput<
  TAccountPayer extends string = string,
  TAccountDeposit extends string = string,
  TAccountCrankProbe extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  payer: TransactionSigner<TAccountPayer>;
  deposit: Address<TAccountDeposit>;
  crankProbe?: Address<TAccountCrankProbe>;
  systemProgram?: Address<TAccountSystemProgram>;
  delaySeconds: InitializeCrankProbeInstructionDataArgs["delaySeconds"];
};

export async function getInitializeCrankProbeInstructionAsync<
  TAccountPayer extends string,
  TAccountDeposit extends string,
  TAccountCrankProbe extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof PROTECTED_PAY_PROGRAM_ADDRESS,
>(
  input: InitializeCrankProbeAsyncInput<
    TAccountPayer,
    TAccountDeposit,
    TAccountCrankProbe,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  InitializeCrankProbeInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountDeposit,
    TAccountCrankProbe,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? PROTECTED_PAY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    payer: { value: input.payer ?? null, isWritable: true },
    deposit: { value: input.deposit ?? null, isWritable: false },
    crankProbe: { value: input.crankProbe ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.crankProbe.value) {
    accounts.crankProbe.value = await findInitializeCrankProbeCrankProbePda(
      {
        payer: getAddressFromResolvedInstructionAccount(
          "payer",
          accounts.payer.value,
        ),
      },
      { programAddress },
    );
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("payer", accounts.payer),
      getAccountMeta("deposit", accounts.deposit),
      getAccountMeta("crankProbe", accounts.crankProbe),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getInitializeCrankProbeInstructionDataEncoder().encode(
      args as InitializeCrankProbeInstructionDataArgs,
    ),
    programAddress,
  } as InitializeCrankProbeInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountDeposit,
    TAccountCrankProbe,
    TAccountSystemProgram
  >);
}

export type InitializeCrankProbeInput<
  TAccountPayer extends string = string,
  TAccountDeposit extends string = string,
  TAccountCrankProbe extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  payer: TransactionSigner<TAccountPayer>;
  deposit: Address<TAccountDeposit>;
  crankProbe: Address<TAccountCrankProbe>;
  systemProgram?: Address<TAccountSystemProgram>;
  delaySeconds: InitializeCrankProbeInstructionDataArgs["delaySeconds"];
};

export function getInitializeCrankProbeInstruction<
  TAccountPayer extends string,
  TAccountDeposit extends string,
  TAccountCrankProbe extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof PROTECTED_PAY_PROGRAM_ADDRESS,
>(
  input: InitializeCrankProbeInput<
    TAccountPayer,
    TAccountDeposit,
    TAccountCrankProbe,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): InitializeCrankProbeInstruction<
  TProgramAddress,
  TAccountPayer,
  TAccountDeposit,
  TAccountCrankProbe,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? PROTECTED_PAY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    payer: { value: input.payer ?? null, isWritable: true },
    deposit: { value: input.deposit ?? null, isWritable: false },
    crankProbe: { value: input.crankProbe ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("payer", accounts.payer),
      getAccountMeta("deposit", accounts.deposit),
      getAccountMeta("crankProbe", accounts.crankProbe),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getInitializeCrankProbeInstructionDataEncoder().encode(
      args as InitializeCrankProbeInstructionDataArgs,
    ),
    programAddress,
  } as InitializeCrankProbeInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountDeposit,
    TAccountCrankProbe,
    TAccountSystemProgram
  >);
}

export type ParsedInitializeCrankProbeInstruction<
  TProgram extends string = typeof PROTECTED_PAY_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    payer: TAccountMetas[0];
    deposit: TAccountMetas[1];
    crankProbe: TAccountMetas[2];
    systemProgram: TAccountMetas[3];
  };
  data: InitializeCrankProbeInstructionData;
};

export function parseInitializeCrankProbeInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedInitializeCrankProbeInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 4,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      payer: getNextAccount(),
      deposit: getNextAccount(),
      crankProbe: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getInitializeCrankProbeInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
