/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findConfigPda, findDepositPda } from "../pdas";
import { PROTECTED_PAY_PROGRAM_ADDRESS } from "../programs";

export const INITIALIZE_DEPOSIT_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([171, 65, 93, 225, 61, 109, 31, 227]);

export function getInitializeDepositDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    INITIALIZE_DEPOSIT_DISCRIMINATOR,
  );
}

export type InitializeDepositInstruction<
  TProgram extends string = typeof PROTECTED_PAY_PROGRAM_ADDRESS,
  TAccountPayer extends string | AccountMeta<string> = string,
  TAccountUser extends string | AccountMeta<string> = string,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountDeposit extends string | AccountMeta<string> = string,
  TAccountTokenMint extends string | AccountMeta<string> = string,
  TAccountTokenProgram extends string | AccountMeta<string> =
    "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA",
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountPayer extends string
        ? WritableSignerAccount<TAccountPayer> &
            AccountSignerMeta<TAccountPayer>
        : TAccountPayer,
      TAccountUser extends string
        ? ReadonlyAccount<TAccountUser>
        : TAccountUser,
      TAccountConfig extends string
        ? ReadonlyAccount<TAccountConfig>
        : TAccountConfig,
      TAccountDeposit extends string
        ? WritableAccount<TAccountDeposit>
        : TAccountDeposit,
      TAccountTokenMint extends string
        ? ReadonlyAccount<TAccountTokenMint>
        : TAccountTokenMint,
      TAccountTokenProgram extends string
        ? ReadonlyAccount<TAccountTokenProgram>
        : TAccountTokenProgram,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type InitializeDepositInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type InitializeDepositInstructionDataArgs = {};

export function getInitializeDepositInstructionDataEncoder(): FixedSizeEncoder<InitializeDepositInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: INITIALIZE_DEPOSIT_DISCRIMINATOR }),
  );
}

export function getInitializeDepositInstructionDataDecoder(): FixedSizeDecoder<InitializeDepositInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getInitializeDepositInstructionDataCodec(): FixedSizeCodec<
  InitializeDepositInstructionDataArgs,
  InitializeDepositInstructionData
> {
  return combineCodec(
    getInitializeDepositInstructionDataEncoder(),
    getInitializeDepositInstructionDataDecoder(),
  );
}

export type InitializeDepositAsyncInput<
  TAccountPayer extends string = string,
  TAccountUser extends string = string,
  TAccountConfig extends string = string,
  TAccountDeposit extends string = string,
  TAccountTokenMint extends string = string,
  TAccountTokenProgram extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  payer: TransactionSigner<TAccountPayer>;
  /** binds this account to the supplied user, and initialization cannot repeat. */
  user: Address<TAccountUser>;
  config?: Address<TAccountConfig>;
  deposit?: Address<TAccountDeposit>;
  tokenMint: Address<TAccountTokenMint>;
  tokenProgram?: Address<TAccountTokenProgram>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export async function getInitializeDepositInstructionAsync<
  TAccountPayer extends string,
  TAccountUser extends string,
  TAccountConfig extends string,
  TAccountDeposit extends string,
  TAccountTokenMint extends string,
  TAccountTokenProgram extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof PROTECTED_PAY_PROGRAM_ADDRESS,
>(
  input: InitializeDepositAsyncInput<
    TAccountPayer,
    TAccountUser,
    TAccountConfig,
    TAccountDeposit,
    TAccountTokenMint,
    TAccountTokenProgram,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  InitializeDepositInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountUser,
    TAccountConfig,
    TAccountDeposit,
    TAccountTokenMint,
    TAccountTokenProgram,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? PROTECTED_PAY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    payer: { value: input.payer ?? null, isWritable: true },
    user: { value: input.user ?? null, isWritable: false },
    config: { value: input.config ?? null, isWritable: false },
    deposit: { value: input.deposit ?? null, isWritable: true },
    tokenMint: { value: input.tokenMint ?? null, isWritable: false },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.config.value) {
    accounts.config.value = await findConfigPda({ programAddress });
  }
  if (!accounts.deposit.value) {
    accounts.deposit.value = await findDepositPda(
      {
        user: getAddressFromResolvedInstructionAccount(
          "user",
          accounts.user.value,
        ),
        tokenMint: getAddressFromResolvedInstructionAccount(
          "tokenMint",
          accounts.tokenMint.value,
        ),
      },
      { programAddress },
    );
  }
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA" as Address<"TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA">;
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("payer", accounts.payer),
      getAccountMeta("user", accounts.user),
      getAccountMeta("config", accounts.config),
      getAccountMeta("deposit", accounts.deposit),
      getAccountMeta("tokenMint", accounts.tokenMint),
      getAccountMeta("tokenProgram", accounts.tokenProgram),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getInitializeDepositInstructionDataEncoder().encode({}),
    programAddress,
  } as InitializeDepositInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountUser,
    TAccountConfig,
    TAccountDeposit,
    TAccountTokenMint,
    TAccountTokenProgram,
    TAccountSystemProgram
  >);
}

export type InitializeDepositInput<
  TAccountPayer extends string = string,
  TAccountUser extends string = string,
  TAccountConfig extends string = string,
  TAccountDeposit extends string = string,
  TAccountTokenMint extends string = string,
  TAccountTokenProgram extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  payer: TransactionSigner<TAccountPayer>;
  /** binds this account to the supplied user, and initialization cannot repeat. */
  user: Address<TAccountUser>;
  config: Address<TAccountConfig>;
  deposit: Address<TAccountDeposit>;
  tokenMint: Address<TAccountTokenMint>;
  tokenProgram?: Address<TAccountTokenProgram>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export function getInitializeDepositInstruction<
  TAccountPayer extends string,
  TAccountUser extends string,
  TAccountConfig extends string,
  TAccountDeposit extends string,
  TAccountTokenMint extends string,
  TAccountTokenProgram extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof PROTECTED_PAY_PROGRAM_ADDRESS,
>(
  input: InitializeDepositInput<
    TAccountPayer,
    TAccountUser,
    TAccountConfig,
    TAccountDeposit,
    TAccountTokenMint,
    TAccountTokenProgram,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): InitializeDepositInstruction<
  TProgramAddress,
  TAccountPayer,
  TAccountUser,
  TAccountConfig,
  TAccountDeposit,
  TAccountTokenMint,
  TAccountTokenProgram,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? PROTECTED_PAY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    payer: { value: input.payer ?? null, isWritable: true },
    user: { value: input.user ?? null, isWritable: false },
    config: { value: input.config ?? null, isWritable: false },
    deposit: { value: input.deposit ?? null, isWritable: true },
    tokenMint: { value: input.tokenMint ?? null, isWritable: false },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA" as Address<"TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA">;
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("payer", accounts.payer),
      getAccountMeta("user", accounts.user),
      getAccountMeta("config", accounts.config),
      getAccountMeta("deposit", accounts.deposit),
      getAccountMeta("tokenMint", accounts.tokenMint),
      getAccountMeta("tokenProgram", accounts.tokenProgram),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getInitializeDepositInstructionDataEncoder().encode({}),
    programAddress,
  } as InitializeDepositInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountUser,
    TAccountConfig,
    TAccountDeposit,
    TAccountTokenMint,
    TAccountTokenProgram,
    TAccountSystemProgram
  >);
}

export type ParsedInitializeDepositInstruction<
  TProgram extends string = typeof PROTECTED_PAY_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    payer: TAccountMetas[0];
    /** binds this account to the supplied user, and initialization cannot repeat. */
    user: TAccountMetas[1];
    config: TAccountMetas[2];
    deposit: TAccountMetas[3];
    tokenMint: TAccountMetas[4];
    tokenProgram: TAccountMetas[5];
    systemProgram: TAccountMetas[6];
  };
  data: InitializeDepositInstructionData;
};

export function parseInitializeDepositInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedInitializeDepositInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 7) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 7,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      payer: getNextAccount(),
      user: getNextAccount(),
      config: getNextAccount(),
      deposit: getNextAccount(),
      tokenMint: getNextAccount(),
      tokenProgram: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getInitializeDepositInstructionDataDecoder().decode(instruction.data),
  };
}
