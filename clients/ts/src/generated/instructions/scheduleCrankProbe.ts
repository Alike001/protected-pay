/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findInitializeCrankProbeCrankProbePda } from "../pdas";
import { PROTECTED_PAY_PROGRAM_ADDRESS } from "../programs";

export const SCHEDULE_CRANK_PROBE_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([127, 49, 87, 118, 193, 63, 90, 84]);

export function getScheduleCrankProbeDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    SCHEDULE_CRANK_PROBE_DISCRIMINATOR,
  );
}

export type ScheduleCrankProbeInstruction<
  TProgram extends string = typeof PROTECTED_PAY_PROGRAM_ADDRESS,
  TAccountMagicProgram extends string | AccountMeta<string> =
    "Magic11111111111111111111111111111111111111",
  TAccountPayer extends string | AccountMeta<string> = string,
  TAccountCrankProbe extends string | AccountMeta<string> = string,
  TAccountDeposit extends string | AccountMeta<string> = string,
  TAccountProgram extends string | AccountMeta<string> =
    "w1ufT3tzJmo6AwLPUV67qXHGTCzUypT7B8RdHATYDGk",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountMagicProgram extends string
        ? ReadonlyAccount<TAccountMagicProgram>
        : TAccountMagicProgram,
      TAccountPayer extends string
        ? WritableSignerAccount<TAccountPayer> &
            AccountSignerMeta<TAccountPayer>
        : TAccountPayer,
      TAccountCrankProbe extends string
        ? WritableAccount<TAccountCrankProbe>
        : TAccountCrankProbe,
      TAccountDeposit extends string
        ? WritableAccount<TAccountDeposit>
        : TAccountDeposit,
      TAccountProgram extends string
        ? ReadonlyAccount<TAccountProgram>
        : TAccountProgram,
      ...TRemainingAccounts,
    ]
  >;

export type ScheduleCrankProbeInstructionData = {
  discriminator: ReadonlyUint8Array;
  taskId: bigint;
  executionIntervalMillis: bigint;
  iterations: bigint;
};

export type ScheduleCrankProbeInstructionDataArgs = {
  taskId: number | bigint;
  executionIntervalMillis: number | bigint;
  iterations: number | bigint;
};

export function getScheduleCrankProbeInstructionDataEncoder(): FixedSizeEncoder<ScheduleCrankProbeInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["taskId", getI64Encoder()],
      ["executionIntervalMillis", getI64Encoder()],
      ["iterations", getI64Encoder()],
    ]),
    (value) => ({
      ...value,
      discriminator: SCHEDULE_CRANK_PROBE_DISCRIMINATOR,
    }),
  );
}

export function getScheduleCrankProbeInstructionDataDecoder(): FixedSizeDecoder<ScheduleCrankProbeInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["taskId", getI64Decoder()],
    ["executionIntervalMillis", getI64Decoder()],
    ["iterations", getI64Decoder()],
  ]);
}

export function getScheduleCrankProbeInstructionDataCodec(): FixedSizeCodec<
  ScheduleCrankProbeInstructionDataArgs,
  ScheduleCrankProbeInstructionData
> {
  return combineCodec(
    getScheduleCrankProbeInstructionDataEncoder(),
    getScheduleCrankProbeInstructionDataDecoder(),
  );
}

export type ScheduleCrankProbeAsyncInput<
  TAccountMagicProgram extends string = string,
  TAccountPayer extends string = string,
  TAccountCrankProbe extends string = string,
  TAccountDeposit extends string = string,
  TAccountProgram extends string = string,
> = {
  magicProgram?: Address<TAccountMagicProgram>;
  payer: TransactionSigner<TAccountPayer>;
  crankProbe?: Address<TAccountCrankProbe>;
  deposit: Address<TAccountDeposit>;
  program?: Address<TAccountProgram>;
  taskId: ScheduleCrankProbeInstructionDataArgs["taskId"];
  executionIntervalMillis: ScheduleCrankProbeInstructionDataArgs["executionIntervalMillis"];
  iterations: ScheduleCrankProbeInstructionDataArgs["iterations"];
};

export async function getScheduleCrankProbeInstructionAsync<
  TAccountMagicProgram extends string,
  TAccountPayer extends string,
  TAccountCrankProbe extends string,
  TAccountDeposit extends string,
  TAccountProgram extends string,
  TProgramAddress extends Address = typeof PROTECTED_PAY_PROGRAM_ADDRESS,
>(
  input: ScheduleCrankProbeAsyncInput<
    TAccountMagicProgram,
    TAccountPayer,
    TAccountCrankProbe,
    TAccountDeposit,
    TAccountProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  ScheduleCrankProbeInstruction<
    TProgramAddress,
    TAccountMagicProgram,
    TAccountPayer,
    TAccountCrankProbe,
    TAccountDeposit,
    TAccountProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? PROTECTED_PAY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    magicProgram: { value: input.magicProgram ?? null, isWritable: false },
    payer: { value: input.payer ?? null, isWritable: true },
    crankProbe: { value: input.crankProbe ?? null, isWritable: true },
    deposit: { value: input.deposit ?? null, isWritable: true },
    program: { value: input.program ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.magicProgram.value) {
    accounts.magicProgram.value =
      "Magic11111111111111111111111111111111111111" as Address<"Magic11111111111111111111111111111111111111">;
  }
  if (!accounts.crankProbe.value) {
    accounts.crankProbe.value = await findInitializeCrankProbeCrankProbePda(
      {
        payer: getAddressFromResolvedInstructionAccount(
          "payer",
          accounts.payer.value,
        ),
      },
      { programAddress },
    );
  }
  if (!accounts.program.value) {
    accounts.program.value =
      "w1ufT3tzJmo6AwLPUV67qXHGTCzUypT7B8RdHATYDGk" as Address<"w1ufT3tzJmo6AwLPUV67qXHGTCzUypT7B8RdHATYDGk">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("magicProgram", accounts.magicProgram),
      getAccountMeta("payer", accounts.payer),
      getAccountMeta("crankProbe", accounts.crankProbe),
      getAccountMeta("deposit", accounts.deposit),
      getAccountMeta("program", accounts.program),
    ],
    data: getScheduleCrankProbeInstructionDataEncoder().encode(
      args as ScheduleCrankProbeInstructionDataArgs,
    ),
    programAddress,
  } as ScheduleCrankProbeInstruction<
    TProgramAddress,
    TAccountMagicProgram,
    TAccountPayer,
    TAccountCrankProbe,
    TAccountDeposit,
    TAccountProgram
  >);
}

export type ScheduleCrankProbeInput<
  TAccountMagicProgram extends string = string,
  TAccountPayer extends string = string,
  TAccountCrankProbe extends string = string,
  TAccountDeposit extends string = string,
  TAccountProgram extends string = string,
> = {
  magicProgram?: Address<TAccountMagicProgram>;
  payer: TransactionSigner<TAccountPayer>;
  crankProbe: Address<TAccountCrankProbe>;
  deposit: Address<TAccountDeposit>;
  program?: Address<TAccountProgram>;
  taskId: ScheduleCrankProbeInstructionDataArgs["taskId"];
  executionIntervalMillis: ScheduleCrankProbeInstructionDataArgs["executionIntervalMillis"];
  iterations: ScheduleCrankProbeInstructionDataArgs["iterations"];
};

export function getScheduleCrankProbeInstruction<
  TAccountMagicProgram extends string,
  TAccountPayer extends string,
  TAccountCrankProbe extends string,
  TAccountDeposit extends string,
  TAccountProgram extends string,
  TProgramAddress extends Address = typeof PROTECTED_PAY_PROGRAM_ADDRESS,
>(
  input: ScheduleCrankProbeInput<
    TAccountMagicProgram,
    TAccountPayer,
    TAccountCrankProbe,
    TAccountDeposit,
    TAccountProgram
  >,
  config?: { programAddress?: TProgramAddress },
): ScheduleCrankProbeInstruction<
  TProgramAddress,
  TAccountMagicProgram,
  TAccountPayer,
  TAccountCrankProbe,
  TAccountDeposit,
  TAccountProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? PROTECTED_PAY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    magicProgram: { value: input.magicProgram ?? null, isWritable: false },
    payer: { value: input.payer ?? null, isWritable: true },
    crankProbe: { value: input.crankProbe ?? null, isWritable: true },
    deposit: { value: input.deposit ?? null, isWritable: true },
    program: { value: input.program ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.magicProgram.value) {
    accounts.magicProgram.value =
      "Magic11111111111111111111111111111111111111" as Address<"Magic11111111111111111111111111111111111111">;
  }
  if (!accounts.program.value) {
    accounts.program.value =
      "w1ufT3tzJmo6AwLPUV67qXHGTCzUypT7B8RdHATYDGk" as Address<"w1ufT3tzJmo6AwLPUV67qXHGTCzUypT7B8RdHATYDGk">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("magicProgram", accounts.magicProgram),
      getAccountMeta("payer", accounts.payer),
      getAccountMeta("crankProbe", accounts.crankProbe),
      getAccountMeta("deposit", accounts.deposit),
      getAccountMeta("program", accounts.program),
    ],
    data: getScheduleCrankProbeInstructionDataEncoder().encode(
      args as ScheduleCrankProbeInstructionDataArgs,
    ),
    programAddress,
  } as ScheduleCrankProbeInstruction<
    TProgramAddress,
    TAccountMagicProgram,
    TAccountPayer,
    TAccountCrankProbe,
    TAccountDeposit,
    TAccountProgram
  >);
}

export type ParsedScheduleCrankProbeInstruction<
  TProgram extends string = typeof PROTECTED_PAY_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    magicProgram: TAccountMetas[0];
    payer: TAccountMetas[1];
    crankProbe: TAccountMetas[2];
    deposit: TAccountMetas[3];
    program: TAccountMetas[4];
  };
  data: ScheduleCrankProbeInstructionData;
};

export function parseScheduleCrankProbeInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedScheduleCrankProbeInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 5,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      magicProgram: getNextAccount(),
      payer: getNextAccount(),
      crankProbe: getNextAccount(),
      deposit: getNextAccount(),
      program: getNextAccount(),
    },
    data: getScheduleCrankProbeInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
