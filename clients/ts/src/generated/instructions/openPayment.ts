/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getNonNullResolvedInstructionInput,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findConfigPda, findPaymentPda } from "../pdas";
import { PROTECTED_PAY_PROGRAM_ADDRESS } from "../programs";

export const OPEN_PAYMENT_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  20, 58, 244, 67, 239, 237, 69, 17,
]);

export function getOpenPaymentDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    OPEN_PAYMENT_DISCRIMINATOR,
  );
}

export type OpenPaymentInstruction<
  TProgram extends string = typeof PROTECTED_PAY_PROGRAM_ADDRESS,
  TAccountSender extends string | AccountMeta<string> = string,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountPayment extends string | AccountMeta<string> = string,
  TAccountSenderDeposit extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountSender extends string
        ? ReadonlySignerAccount<TAccountSender> &
            AccountSignerMeta<TAccountSender>
        : TAccountSender,
      TAccountConfig extends string
        ? ReadonlyAccount<TAccountConfig>
        : TAccountConfig,
      TAccountPayment extends string
        ? WritableAccount<TAccountPayment>
        : TAccountPayment,
      TAccountSenderDeposit extends string
        ? WritableAccount<TAccountSenderDeposit>
        : TAccountSenderDeposit,
      ...TRemainingAccounts,
    ]
  >;

export type OpenPaymentInstructionData = {
  discriminator: ReadonlyUint8Array;
  paymentId: ReadonlyUint8Array;
  amount: bigint;
  memoHash: ReadonlyUint8Array;
};

export type OpenPaymentInstructionDataArgs = {
  paymentId: ReadonlyUint8Array;
  amount: number | bigint;
  memoHash: ReadonlyUint8Array;
};

export function getOpenPaymentInstructionDataEncoder(): FixedSizeEncoder<OpenPaymentInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["paymentId", fixEncoderSize(getBytesEncoder(), 32)],
      ["amount", getU64Encoder()],
      ["memoHash", fixEncoderSize(getBytesEncoder(), 32)],
    ]),
    (value) => ({ ...value, discriminator: OPEN_PAYMENT_DISCRIMINATOR }),
  );
}

export function getOpenPaymentInstructionDataDecoder(): FixedSizeDecoder<OpenPaymentInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["paymentId", fixDecoderSize(getBytesDecoder(), 32)],
    ["amount", getU64Decoder()],
    ["memoHash", fixDecoderSize(getBytesDecoder(), 32)],
  ]);
}

export function getOpenPaymentInstructionDataCodec(): FixedSizeCodec<
  OpenPaymentInstructionDataArgs,
  OpenPaymentInstructionData
> {
  return combineCodec(
    getOpenPaymentInstructionDataEncoder(),
    getOpenPaymentInstructionDataDecoder(),
  );
}

export type OpenPaymentAsyncInput<
  TAccountSender extends string = string,
  TAccountConfig extends string = string,
  TAccountPayment extends string = string,
  TAccountSenderDeposit extends string = string,
> = {
  sender: TransactionSigner<TAccountSender>;
  config?: Address<TAccountConfig>;
  payment?: Address<TAccountPayment>;
  senderDeposit: Address<TAccountSenderDeposit>;
  paymentId: OpenPaymentInstructionDataArgs["paymentId"];
  amount: OpenPaymentInstructionDataArgs["amount"];
  memoHash: OpenPaymentInstructionDataArgs["memoHash"];
};

export async function getOpenPaymentInstructionAsync<
  TAccountSender extends string,
  TAccountConfig extends string,
  TAccountPayment extends string,
  TAccountSenderDeposit extends string,
  TProgramAddress extends Address = typeof PROTECTED_PAY_PROGRAM_ADDRESS,
>(
  input: OpenPaymentAsyncInput<
    TAccountSender,
    TAccountConfig,
    TAccountPayment,
    TAccountSenderDeposit
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  OpenPaymentInstruction<
    TProgramAddress,
    TAccountSender,
    TAccountConfig,
    TAccountPayment,
    TAccountSenderDeposit
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? PROTECTED_PAY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    sender: { value: input.sender ?? null, isWritable: false },
    config: { value: input.config ?? null, isWritable: false },
    payment: { value: input.payment ?? null, isWritable: true },
    senderDeposit: { value: input.senderDeposit ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.config.value) {
    accounts.config.value = await findConfigPda({ programAddress });
  }
  if (!accounts.payment.value) {
    accounts.payment.value = await findPaymentPda(
      {
        paymentId: getNonNullResolvedInstructionInput(
          "paymentId",
          args.paymentId,
        ),
      },
      { programAddress },
    );
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("sender", accounts.sender),
      getAccountMeta("config", accounts.config),
      getAccountMeta("payment", accounts.payment),
      getAccountMeta("senderDeposit", accounts.senderDeposit),
    ],
    data: getOpenPaymentInstructionDataEncoder().encode(
      args as OpenPaymentInstructionDataArgs,
    ),
    programAddress,
  } as OpenPaymentInstruction<
    TProgramAddress,
    TAccountSender,
    TAccountConfig,
    TAccountPayment,
    TAccountSenderDeposit
  >);
}

export type OpenPaymentInput<
  TAccountSender extends string = string,
  TAccountConfig extends string = string,
  TAccountPayment extends string = string,
  TAccountSenderDeposit extends string = string,
> = {
  sender: TransactionSigner<TAccountSender>;
  config: Address<TAccountConfig>;
  payment: Address<TAccountPayment>;
  senderDeposit: Address<TAccountSenderDeposit>;
  paymentId: OpenPaymentInstructionDataArgs["paymentId"];
  amount: OpenPaymentInstructionDataArgs["amount"];
  memoHash: OpenPaymentInstructionDataArgs["memoHash"];
};

export function getOpenPaymentInstruction<
  TAccountSender extends string,
  TAccountConfig extends string,
  TAccountPayment extends string,
  TAccountSenderDeposit extends string,
  TProgramAddress extends Address = typeof PROTECTED_PAY_PROGRAM_ADDRESS,
>(
  input: OpenPaymentInput<
    TAccountSender,
    TAccountConfig,
    TAccountPayment,
    TAccountSenderDeposit
  >,
  config?: { programAddress?: TProgramAddress },
): OpenPaymentInstruction<
  TProgramAddress,
  TAccountSender,
  TAccountConfig,
  TAccountPayment,
  TAccountSenderDeposit
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? PROTECTED_PAY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    sender: { value: input.sender ?? null, isWritable: false },
    config: { value: input.config ?? null, isWritable: false },
    payment: { value: input.payment ?? null, isWritable: true },
    senderDeposit: { value: input.senderDeposit ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("sender", accounts.sender),
      getAccountMeta("config", accounts.config),
      getAccountMeta("payment", accounts.payment),
      getAccountMeta("senderDeposit", accounts.senderDeposit),
    ],
    data: getOpenPaymentInstructionDataEncoder().encode(
      args as OpenPaymentInstructionDataArgs,
    ),
    programAddress,
  } as OpenPaymentInstruction<
    TProgramAddress,
    TAccountSender,
    TAccountConfig,
    TAccountPayment,
    TAccountSenderDeposit
  >);
}

export type ParsedOpenPaymentInstruction<
  TProgram extends string = typeof PROTECTED_PAY_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    sender: TAccountMetas[0];
    config: TAccountMetas[1];
    payment: TAccountMetas[2];
    senderDeposit: TAccountMetas[3];
  };
  data: OpenPaymentInstructionData;
};

export function parseOpenPaymentInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedOpenPaymentInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 4,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      sender: getNextAccount(),
      config: getNextAccount(),
      payment: getNextAccount(),
      senderDeposit: getNextAccount(),
    },
    data: getOpenPaymentInstructionDataDecoder().decode(instruction.data),
  };
}
