/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getNonNullResolvedInstructionInput,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findConfigPda, findPaymentPda } from "../pdas";
import { PROTECTED_PAY_PROGRAM_ADDRESS } from "../programs";

export const PREPARE_PAYMENT_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array(
  [216, 180, 241, 36, 108, 90, 166, 23],
);

export function getPreparePaymentDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    PREPARE_PAYMENT_DISCRIMINATOR,
  );
}

export type PreparePaymentInstruction<
  TProgram extends string = typeof PROTECTED_PAY_PROGRAM_ADDRESS,
  TAccountSender extends string | AccountMeta<string> = string,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountPayment extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountSender extends string
        ? WritableSignerAccount<TAccountSender> &
            AccountSignerMeta<TAccountSender>
        : TAccountSender,
      TAccountConfig extends string
        ? ReadonlyAccount<TAccountConfig>
        : TAccountConfig,
      TAccountPayment extends string
        ? WritableAccount<TAccountPayment>
        : TAccountPayment,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type PreparePaymentInstructionData = {
  discriminator: ReadonlyUint8Array;
  paymentId: ReadonlyUint8Array;
  recipient: Address;
};

export type PreparePaymentInstructionDataArgs = {
  paymentId: ReadonlyUint8Array;
  recipient: Address;
};

export function getPreparePaymentInstructionDataEncoder(): FixedSizeEncoder<PreparePaymentInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["paymentId", fixEncoderSize(getBytesEncoder(), 32)],
      ["recipient", getAddressEncoder()],
    ]),
    (value) => ({ ...value, discriminator: PREPARE_PAYMENT_DISCRIMINATOR }),
  );
}

export function getPreparePaymentInstructionDataDecoder(): FixedSizeDecoder<PreparePaymentInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["paymentId", fixDecoderSize(getBytesDecoder(), 32)],
    ["recipient", getAddressDecoder()],
  ]);
}

export function getPreparePaymentInstructionDataCodec(): FixedSizeCodec<
  PreparePaymentInstructionDataArgs,
  PreparePaymentInstructionData
> {
  return combineCodec(
    getPreparePaymentInstructionDataEncoder(),
    getPreparePaymentInstructionDataDecoder(),
  );
}

export type PreparePaymentAsyncInput<
  TAccountSender extends string = string,
  TAccountConfig extends string = string,
  TAccountPayment extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  sender: TransactionSigner<TAccountSender>;
  config?: Address<TAccountConfig>;
  payment?: Address<TAccountPayment>;
  systemProgram?: Address<TAccountSystemProgram>;
  paymentId: PreparePaymentInstructionDataArgs["paymentId"];
  recipient: PreparePaymentInstructionDataArgs["recipient"];
};

export async function getPreparePaymentInstructionAsync<
  TAccountSender extends string,
  TAccountConfig extends string,
  TAccountPayment extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof PROTECTED_PAY_PROGRAM_ADDRESS,
>(
  input: PreparePaymentAsyncInput<
    TAccountSender,
    TAccountConfig,
    TAccountPayment,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  PreparePaymentInstruction<
    TProgramAddress,
    TAccountSender,
    TAccountConfig,
    TAccountPayment,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? PROTECTED_PAY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    sender: { value: input.sender ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: false },
    payment: { value: input.payment ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.config.value) {
    accounts.config.value = await findConfigPda({ programAddress });
  }
  if (!accounts.payment.value) {
    accounts.payment.value = await findPaymentPda(
      {
        paymentId: getNonNullResolvedInstructionInput(
          "paymentId",
          args.paymentId,
        ),
      },
      { programAddress },
    );
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("sender", accounts.sender),
      getAccountMeta("config", accounts.config),
      getAccountMeta("payment", accounts.payment),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getPreparePaymentInstructionDataEncoder().encode(
      args as PreparePaymentInstructionDataArgs,
    ),
    programAddress,
  } as PreparePaymentInstruction<
    TProgramAddress,
    TAccountSender,
    TAccountConfig,
    TAccountPayment,
    TAccountSystemProgram
  >);
}

export type PreparePaymentInput<
  TAccountSender extends string = string,
  TAccountConfig extends string = string,
  TAccountPayment extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  sender: TransactionSigner<TAccountSender>;
  config: Address<TAccountConfig>;
  payment: Address<TAccountPayment>;
  systemProgram?: Address<TAccountSystemProgram>;
  paymentId: PreparePaymentInstructionDataArgs["paymentId"];
  recipient: PreparePaymentInstructionDataArgs["recipient"];
};

export function getPreparePaymentInstruction<
  TAccountSender extends string,
  TAccountConfig extends string,
  TAccountPayment extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof PROTECTED_PAY_PROGRAM_ADDRESS,
>(
  input: PreparePaymentInput<
    TAccountSender,
    TAccountConfig,
    TAccountPayment,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): PreparePaymentInstruction<
  TProgramAddress,
  TAccountSender,
  TAccountConfig,
  TAccountPayment,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? PROTECTED_PAY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    sender: { value: input.sender ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: false },
    payment: { value: input.payment ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("sender", accounts.sender),
      getAccountMeta("config", accounts.config),
      getAccountMeta("payment", accounts.payment),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getPreparePaymentInstructionDataEncoder().encode(
      args as PreparePaymentInstructionDataArgs,
    ),
    programAddress,
  } as PreparePaymentInstruction<
    TProgramAddress,
    TAccountSender,
    TAccountConfig,
    TAccountPayment,
    TAccountSystemProgram
  >);
}

export type ParsedPreparePaymentInstruction<
  TProgram extends string = typeof PROTECTED_PAY_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    sender: TAccountMetas[0];
    config: TAccountMetas[1];
    payment: TAccountMetas[2];
    systemProgram: TAccountMetas[3];
  };
  data: PreparePaymentInstructionData;
};

export function parsePreparePaymentInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedPreparePaymentInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 4,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      sender: getNextAccount(),
      config: getNextAccount(),
      payment: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getPreparePaymentInstructionDataDecoder().decode(instruction.data),
  };
}
