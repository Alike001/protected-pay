/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { PROTECTED_PAY_PROGRAM_ADDRESS } from "../programs";

export const REDACT_TERMINAL_PAYMENT_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([27, 147, 68, 32, 195, 51, 127, 216]);

export function getRedactTerminalPaymentDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    REDACT_TERMINAL_PAYMENT_DISCRIMINATOR,
  );
}

export type RedactTerminalPaymentInstruction<
  TProgram extends string = typeof PROTECTED_PAY_PROGRAM_ADDRESS,
  TAccountSender extends string | AccountMeta<string> = string,
  TAccountPayment extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountSender extends string
        ? ReadonlySignerAccount<TAccountSender> &
            AccountSignerMeta<TAccountSender>
        : TAccountSender,
      TAccountPayment extends string
        ? WritableAccount<TAccountPayment>
        : TAccountPayment,
      ...TRemainingAccounts,
    ]
  >;

export type RedactTerminalPaymentInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type RedactTerminalPaymentInstructionDataArgs = {};

export function getRedactTerminalPaymentInstructionDataEncoder(): FixedSizeEncoder<RedactTerminalPaymentInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({
      ...value,
      discriminator: REDACT_TERMINAL_PAYMENT_DISCRIMINATOR,
    }),
  );
}

export function getRedactTerminalPaymentInstructionDataDecoder(): FixedSizeDecoder<RedactTerminalPaymentInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getRedactTerminalPaymentInstructionDataCodec(): FixedSizeCodec<
  RedactTerminalPaymentInstructionDataArgs,
  RedactTerminalPaymentInstructionData
> {
  return combineCodec(
    getRedactTerminalPaymentInstructionDataEncoder(),
    getRedactTerminalPaymentInstructionDataDecoder(),
  );
}

export type RedactTerminalPaymentInput<
  TAccountSender extends string = string,
  TAccountPayment extends string = string,
> = {
  sender: TransactionSigner<TAccountSender>;
  payment: Address<TAccountPayment>;
};

export function getRedactTerminalPaymentInstruction<
  TAccountSender extends string,
  TAccountPayment extends string,
  TProgramAddress extends Address = typeof PROTECTED_PAY_PROGRAM_ADDRESS,
>(
  input: RedactTerminalPaymentInput<TAccountSender, TAccountPayment>,
  config?: { programAddress?: TProgramAddress },
): RedactTerminalPaymentInstruction<
  TProgramAddress,
  TAccountSender,
  TAccountPayment
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? PROTECTED_PAY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    sender: { value: input.sender ?? null, isWritable: false },
    payment: { value: input.payment ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("sender", accounts.sender),
      getAccountMeta("payment", accounts.payment),
    ],
    data: getRedactTerminalPaymentInstructionDataEncoder().encode({}),
    programAddress,
  } as RedactTerminalPaymentInstruction<
    TProgramAddress,
    TAccountSender,
    TAccountPayment
  >);
}

export type ParsedRedactTerminalPaymentInstruction<
  TProgram extends string = typeof PROTECTED_PAY_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    sender: TAccountMetas[0];
    payment: TAccountMetas[1];
  };
  data: RedactTerminalPaymentInstructionData;
};

export function parseRedactTerminalPaymentInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedRedactTerminalPaymentInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 2,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { sender: getNextAccount(), payment: getNextAccount() },
    data: getRedactTerminalPaymentInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
