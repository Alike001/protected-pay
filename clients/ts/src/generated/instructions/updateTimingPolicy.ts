/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findConfigPda } from "../pdas";
import { PROTECTED_PAY_PROGRAM_ADDRESS } from "../programs";

export const UPDATE_TIMING_POLICY_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([225, 178, 23, 10, 35, 19, 150, 143]);

export function getUpdateTimingPolicyDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    UPDATE_TIMING_POLICY_DISCRIMINATOR,
  );
}

export type UpdateTimingPolicyInstruction<
  TProgram extends string = typeof PROTECTED_PAY_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountConfig extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountConfig extends string
        ? WritableAccount<TAccountConfig>
        : TAccountConfig,
      ...TRemainingAccounts,
    ]
  >;

export type UpdateTimingPolicyInstructionData = {
  discriminator: ReadonlyUint8Array;
  safetyWindowSeconds: bigint;
  claimWindowSeconds: bigint;
};

export type UpdateTimingPolicyInstructionDataArgs = {
  safetyWindowSeconds: number | bigint;
  claimWindowSeconds: number | bigint;
};

export function getUpdateTimingPolicyInstructionDataEncoder(): FixedSizeEncoder<UpdateTimingPolicyInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["safetyWindowSeconds", getI64Encoder()],
      ["claimWindowSeconds", getI64Encoder()],
    ]),
    (value) => ({
      ...value,
      discriminator: UPDATE_TIMING_POLICY_DISCRIMINATOR,
    }),
  );
}

export function getUpdateTimingPolicyInstructionDataDecoder(): FixedSizeDecoder<UpdateTimingPolicyInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["safetyWindowSeconds", getI64Decoder()],
    ["claimWindowSeconds", getI64Decoder()],
  ]);
}

export function getUpdateTimingPolicyInstructionDataCodec(): FixedSizeCodec<
  UpdateTimingPolicyInstructionDataArgs,
  UpdateTimingPolicyInstructionData
> {
  return combineCodec(
    getUpdateTimingPolicyInstructionDataEncoder(),
    getUpdateTimingPolicyInstructionDataDecoder(),
  );
}

export type UpdateTimingPolicyAsyncInput<
  TAccountAuthority extends string = string,
  TAccountConfig extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  config?: Address<TAccountConfig>;
  safetyWindowSeconds: UpdateTimingPolicyInstructionDataArgs["safetyWindowSeconds"];
  claimWindowSeconds: UpdateTimingPolicyInstructionDataArgs["claimWindowSeconds"];
};

export async function getUpdateTimingPolicyInstructionAsync<
  TAccountAuthority extends string,
  TAccountConfig extends string,
  TProgramAddress extends Address = typeof PROTECTED_PAY_PROGRAM_ADDRESS,
>(
  input: UpdateTimingPolicyAsyncInput<TAccountAuthority, TAccountConfig>,
  config?: { programAddress?: TProgramAddress },
): Promise<
  UpdateTimingPolicyInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountConfig
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? PROTECTED_PAY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: false },
    config: { value: input.config ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.config.value) {
    accounts.config.value = await findConfigPda({ programAddress });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("config", accounts.config),
    ],
    data: getUpdateTimingPolicyInstructionDataEncoder().encode(
      args as UpdateTimingPolicyInstructionDataArgs,
    ),
    programAddress,
  } as UpdateTimingPolicyInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountConfig
  >);
}

export type UpdateTimingPolicyInput<
  TAccountAuthority extends string = string,
  TAccountConfig extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  config: Address<TAccountConfig>;
  safetyWindowSeconds: UpdateTimingPolicyInstructionDataArgs["safetyWindowSeconds"];
  claimWindowSeconds: UpdateTimingPolicyInstructionDataArgs["claimWindowSeconds"];
};

export function getUpdateTimingPolicyInstruction<
  TAccountAuthority extends string,
  TAccountConfig extends string,
  TProgramAddress extends Address = typeof PROTECTED_PAY_PROGRAM_ADDRESS,
>(
  input: UpdateTimingPolicyInput<TAccountAuthority, TAccountConfig>,
  config?: { programAddress?: TProgramAddress },
): UpdateTimingPolicyInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountConfig
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? PROTECTED_PAY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: false },
    config: { value: input.config ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("config", accounts.config),
    ],
    data: getUpdateTimingPolicyInstructionDataEncoder().encode(
      args as UpdateTimingPolicyInstructionDataArgs,
    ),
    programAddress,
  } as UpdateTimingPolicyInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountConfig
  >);
}

export type ParsedUpdateTimingPolicyInstruction<
  TProgram extends string = typeof PROTECTED_PAY_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    config: TAccountMetas[1];
  };
  data: UpdateTimingPolicyInstructionData;
};

export function parseUpdateTimingPolicyInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedUpdateTimingPolicyInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 2,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { authority: getNextAccount(), config: getNextAccount() },
    data: getUpdateTimingPolicyInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
