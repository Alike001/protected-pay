/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getNonNullResolvedInstructionInput,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findPaymentPda } from "../pdas";
import { PROTECTED_PAY_PROGRAM_ADDRESS } from "../programs";

export const SCHEDULE_PAYMENT_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([9, 34, 138, 251, 220, 61, 94, 17]);

export function getSchedulePaymentDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    SCHEDULE_PAYMENT_DISCRIMINATOR,
  );
}

export type SchedulePaymentInstruction<
  TProgram extends string = typeof PROTECTED_PAY_PROGRAM_ADDRESS,
  TAccountMagicProgram extends string | AccountMeta<string> =
    "Magic11111111111111111111111111111111111111",
  TAccountPayer extends string | AccountMeta<string> = string,
  TAccountSender extends string | AccountMeta<string> = string,
  TAccountSessionToken extends string | AccountMeta<string> = string,
  TAccountPayment extends string | AccountMeta<string> = string,
  TAccountProgram extends string | AccountMeta<string> =
    "w1ufT3tzJmo6AwLPUV67qXHGTCzUypT7B8RdHATYDGk",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountMagicProgram extends string
        ? ReadonlyAccount<TAccountMagicProgram>
        : TAccountMagicProgram,
      TAccountPayer extends string
        ? WritableSignerAccount<TAccountPayer> &
            AccountSignerMeta<TAccountPayer>
        : TAccountPayer,
      TAccountSender extends string
        ? ReadonlyAccount<TAccountSender>
        : TAccountSender,
      TAccountSessionToken extends string
        ? ReadonlyAccount<TAccountSessionToken>
        : TAccountSessionToken,
      TAccountPayment extends string
        ? WritableAccount<TAccountPayment>
        : TAccountPayment,
      TAccountProgram extends string
        ? ReadonlyAccount<TAccountProgram>
        : TAccountProgram,
      ...TRemainingAccounts,
    ]
  >;

export type SchedulePaymentInstructionData = {
  discriminator: ReadonlyUint8Array;
  paymentId: ReadonlyUint8Array;
  taskId: bigint;
  executionIntervalMillis: bigint;
  iterations: bigint;
};

export type SchedulePaymentInstructionDataArgs = {
  paymentId: ReadonlyUint8Array;
  taskId: number | bigint;
  executionIntervalMillis: number | bigint;
  iterations: number | bigint;
};

export function getSchedulePaymentInstructionDataEncoder(): FixedSizeEncoder<SchedulePaymentInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["paymentId", fixEncoderSize(getBytesEncoder(), 32)],
      ["taskId", getI64Encoder()],
      ["executionIntervalMillis", getI64Encoder()],
      ["iterations", getI64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: SCHEDULE_PAYMENT_DISCRIMINATOR }),
  );
}

export function getSchedulePaymentInstructionDataDecoder(): FixedSizeDecoder<SchedulePaymentInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["paymentId", fixDecoderSize(getBytesDecoder(), 32)],
    ["taskId", getI64Decoder()],
    ["executionIntervalMillis", getI64Decoder()],
    ["iterations", getI64Decoder()],
  ]);
}

export function getSchedulePaymentInstructionDataCodec(): FixedSizeCodec<
  SchedulePaymentInstructionDataArgs,
  SchedulePaymentInstructionData
> {
  return combineCodec(
    getSchedulePaymentInstructionDataEncoder(),
    getSchedulePaymentInstructionDataDecoder(),
  );
}

export type SchedulePaymentAsyncInput<
  TAccountMagicProgram extends string = string,
  TAccountPayer extends string = string,
  TAccountSender extends string = string,
  TAccountSessionToken extends string = string,
  TAccountPayment extends string = string,
  TAccountProgram extends string = string,
> = {
  magicProgram?: Address<TAccountMagicProgram>;
  payer: TransactionSigner<TAccountPayer>;
  sender: Address<TAccountSender>;
  sessionToken?: Address<TAccountSessionToken>;
  payment?: Address<TAccountPayment>;
  program?: Address<TAccountProgram>;
  paymentId: SchedulePaymentInstructionDataArgs["paymentId"];
  taskId: SchedulePaymentInstructionDataArgs["taskId"];
  executionIntervalMillis: SchedulePaymentInstructionDataArgs["executionIntervalMillis"];
  iterations: SchedulePaymentInstructionDataArgs["iterations"];
};

export async function getSchedulePaymentInstructionAsync<
  TAccountMagicProgram extends string,
  TAccountPayer extends string,
  TAccountSender extends string,
  TAccountSessionToken extends string,
  TAccountPayment extends string,
  TAccountProgram extends string,
  TProgramAddress extends Address = typeof PROTECTED_PAY_PROGRAM_ADDRESS,
>(
  input: SchedulePaymentAsyncInput<
    TAccountMagicProgram,
    TAccountPayer,
    TAccountSender,
    TAccountSessionToken,
    TAccountPayment,
    TAccountProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  SchedulePaymentInstruction<
    TProgramAddress,
    TAccountMagicProgram,
    TAccountPayer,
    TAccountSender,
    TAccountSessionToken,
    TAccountPayment,
    TAccountProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? PROTECTED_PAY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    magicProgram: { value: input.magicProgram ?? null, isWritable: false },
    payer: { value: input.payer ?? null, isWritable: true },
    sender: { value: input.sender ?? null, isWritable: false },
    sessionToken: { value: input.sessionToken ?? null, isWritable: false },
    payment: { value: input.payment ?? null, isWritable: true },
    program: { value: input.program ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.magicProgram.value) {
    accounts.magicProgram.value =
      "Magic11111111111111111111111111111111111111" as Address<"Magic11111111111111111111111111111111111111">;
  }
  if (!accounts.payment.value) {
    accounts.payment.value = await findPaymentPda(
      {
        paymentId: getNonNullResolvedInstructionInput(
          "paymentId",
          args.paymentId,
        ),
      },
      { programAddress },
    );
  }
  if (!accounts.program.value) {
    accounts.program.value =
      "w1ufT3tzJmo6AwLPUV67qXHGTCzUypT7B8RdHATYDGk" as Address<"w1ufT3tzJmo6AwLPUV67qXHGTCzUypT7B8RdHATYDGk">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("magicProgram", accounts.magicProgram),
      getAccountMeta("payer", accounts.payer),
      getAccountMeta("sender", accounts.sender),
      getAccountMeta("sessionToken", accounts.sessionToken),
      getAccountMeta("payment", accounts.payment),
      getAccountMeta("program", accounts.program),
    ],
    data: getSchedulePaymentInstructionDataEncoder().encode(
      args as SchedulePaymentInstructionDataArgs,
    ),
    programAddress,
  } as SchedulePaymentInstruction<
    TProgramAddress,
    TAccountMagicProgram,
    TAccountPayer,
    TAccountSender,
    TAccountSessionToken,
    TAccountPayment,
    TAccountProgram
  >);
}

export type SchedulePaymentInput<
  TAccountMagicProgram extends string = string,
  TAccountPayer extends string = string,
  TAccountSender extends string = string,
  TAccountSessionToken extends string = string,
  TAccountPayment extends string = string,
  TAccountProgram extends string = string,
> = {
  magicProgram?: Address<TAccountMagicProgram>;
  payer: TransactionSigner<TAccountPayer>;
  sender: Address<TAccountSender>;
  sessionToken?: Address<TAccountSessionToken>;
  payment: Address<TAccountPayment>;
  program?: Address<TAccountProgram>;
  paymentId: SchedulePaymentInstructionDataArgs["paymentId"];
  taskId: SchedulePaymentInstructionDataArgs["taskId"];
  executionIntervalMillis: SchedulePaymentInstructionDataArgs["executionIntervalMillis"];
  iterations: SchedulePaymentInstructionDataArgs["iterations"];
};

export function getSchedulePaymentInstruction<
  TAccountMagicProgram extends string,
  TAccountPayer extends string,
  TAccountSender extends string,
  TAccountSessionToken extends string,
  TAccountPayment extends string,
  TAccountProgram extends string,
  TProgramAddress extends Address = typeof PROTECTED_PAY_PROGRAM_ADDRESS,
>(
  input: SchedulePaymentInput<
    TAccountMagicProgram,
    TAccountPayer,
    TAccountSender,
    TAccountSessionToken,
    TAccountPayment,
    TAccountProgram
  >,
  config?: { programAddress?: TProgramAddress },
): SchedulePaymentInstruction<
  TProgramAddress,
  TAccountMagicProgram,
  TAccountPayer,
  TAccountSender,
  TAccountSessionToken,
  TAccountPayment,
  TAccountProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? PROTECTED_PAY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    magicProgram: { value: input.magicProgram ?? null, isWritable: false },
    payer: { value: input.payer ?? null, isWritable: true },
    sender: { value: input.sender ?? null, isWritable: false },
    sessionToken: { value: input.sessionToken ?? null, isWritable: false },
    payment: { value: input.payment ?? null, isWritable: true },
    program: { value: input.program ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.magicProgram.value) {
    accounts.magicProgram.value =
      "Magic11111111111111111111111111111111111111" as Address<"Magic11111111111111111111111111111111111111">;
  }
  if (!accounts.program.value) {
    accounts.program.value =
      "w1ufT3tzJmo6AwLPUV67qXHGTCzUypT7B8RdHATYDGk" as Address<"w1ufT3tzJmo6AwLPUV67qXHGTCzUypT7B8RdHATYDGk">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("magicProgram", accounts.magicProgram),
      getAccountMeta("payer", accounts.payer),
      getAccountMeta("sender", accounts.sender),
      getAccountMeta("sessionToken", accounts.sessionToken),
      getAccountMeta("payment", accounts.payment),
      getAccountMeta("program", accounts.program),
    ],
    data: getSchedulePaymentInstructionDataEncoder().encode(
      args as SchedulePaymentInstructionDataArgs,
    ),
    programAddress,
  } as SchedulePaymentInstruction<
    TProgramAddress,
    TAccountMagicProgram,
    TAccountPayer,
    TAccountSender,
    TAccountSessionToken,
    TAccountPayment,
    TAccountProgram
  >);
}

export type ParsedSchedulePaymentInstruction<
  TProgram extends string = typeof PROTECTED_PAY_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    magicProgram: TAccountMetas[0];
    payer: TAccountMetas[1];
    sender: TAccountMetas[2];
    sessionToken?: TAccountMetas[3] | undefined;
    payment: TAccountMetas[4];
    program: TAccountMetas[5];
  };
  data: SchedulePaymentInstructionData;
};

export function parseSchedulePaymentInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedSchedulePaymentInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 6) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 6,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  const getNextOptionalAccount = () => {
    const accountMeta = getNextAccount();
    return accountMeta.address === PROTECTED_PAY_PROGRAM_ADDRESS
      ? undefined
      : accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      magicProgram: getNextAccount(),
      payer: getNextAccount(),
      sender: getNextAccount(),
      sessionToken: getNextOptionalAccount(),
      payment: getNextAccount(),
      program: getNextAccount(),
    },
    data: getSchedulePaymentInstructionDataDecoder().decode(instruction.data),
  };
}
