/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findCrankProbePda } from "../pdas";
import { PROTECTED_PAY_PROGRAM_ADDRESS } from "../programs";

export const COMMIT_AND_UNDELEGATE_CRANK_PROBE_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([3, 51, 89, 221, 144, 207, 148, 84]);

export function getCommitAndUndelegateCrankProbeDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    COMMIT_AND_UNDELEGATE_CRANK_PROBE_DISCRIMINATOR,
  );
}

export type CommitAndUndelegateCrankProbeInstruction<
  TProgram extends string = typeof PROTECTED_PAY_PROGRAM_ADDRESS,
  TAccountPayer extends string | AccountMeta<string> = string,
  TAccountOwner extends string | AccountMeta<string> = string,
  TAccountCrankProbe extends string | AccountMeta<string> = string,
  TAccountDeposit extends string | AccountMeta<string> = string,
  TAccountMagicProgram extends string | AccountMeta<string> =
    "Magic11111111111111111111111111111111111111",
  TAccountMagicContext extends string | AccountMeta<string> =
    "MagicContext1111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountPayer extends string
        ? WritableSignerAccount<TAccountPayer> &
            AccountSignerMeta<TAccountPayer>
        : TAccountPayer,
      TAccountOwner extends string
        ? ReadonlySignerAccount<TAccountOwner> &
            AccountSignerMeta<TAccountOwner>
        : TAccountOwner,
      TAccountCrankProbe extends string
        ? WritableAccount<TAccountCrankProbe>
        : TAccountCrankProbe,
      TAccountDeposit extends string
        ? WritableAccount<TAccountDeposit>
        : TAccountDeposit,
      TAccountMagicProgram extends string
        ? ReadonlyAccount<TAccountMagicProgram>
        : TAccountMagicProgram,
      TAccountMagicContext extends string
        ? WritableAccount<TAccountMagicContext>
        : TAccountMagicContext,
      ...TRemainingAccounts,
    ]
  >;

export type CommitAndUndelegateCrankProbeInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type CommitAndUndelegateCrankProbeInstructionDataArgs = {};

export function getCommitAndUndelegateCrankProbeInstructionDataEncoder(): FixedSizeEncoder<CommitAndUndelegateCrankProbeInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({
      ...value,
      discriminator: COMMIT_AND_UNDELEGATE_CRANK_PROBE_DISCRIMINATOR,
    }),
  );
}

export function getCommitAndUndelegateCrankProbeInstructionDataDecoder(): FixedSizeDecoder<CommitAndUndelegateCrankProbeInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getCommitAndUndelegateCrankProbeInstructionDataCodec(): FixedSizeCodec<
  CommitAndUndelegateCrankProbeInstructionDataArgs,
  CommitAndUndelegateCrankProbeInstructionData
> {
  return combineCodec(
    getCommitAndUndelegateCrankProbeInstructionDataEncoder(),
    getCommitAndUndelegateCrankProbeInstructionDataDecoder(),
  );
}

export type CommitAndUndelegateCrankProbeAsyncInput<
  TAccountPayer extends string = string,
  TAccountOwner extends string = string,
  TAccountCrankProbe extends string = string,
  TAccountDeposit extends string = string,
  TAccountMagicProgram extends string = string,
  TAccountMagicContext extends string = string,
> = {
  payer: TransactionSigner<TAccountPayer>;
  owner: TransactionSigner<TAccountOwner>;
  crankProbe?: Address<TAccountCrankProbe>;
  deposit: Address<TAccountDeposit>;
  magicProgram?: Address<TAccountMagicProgram>;
  magicContext?: Address<TAccountMagicContext>;
};

export async function getCommitAndUndelegateCrankProbeInstructionAsync<
  TAccountPayer extends string,
  TAccountOwner extends string,
  TAccountCrankProbe extends string,
  TAccountDeposit extends string,
  TAccountMagicProgram extends string,
  TAccountMagicContext extends string,
  TProgramAddress extends Address = typeof PROTECTED_PAY_PROGRAM_ADDRESS,
>(
  input: CommitAndUndelegateCrankProbeAsyncInput<
    TAccountPayer,
    TAccountOwner,
    TAccountCrankProbe,
    TAccountDeposit,
    TAccountMagicProgram,
    TAccountMagicContext
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  CommitAndUndelegateCrankProbeInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountOwner,
    TAccountCrankProbe,
    TAccountDeposit,
    TAccountMagicProgram,
    TAccountMagicContext
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? PROTECTED_PAY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    payer: { value: input.payer ?? null, isWritable: true },
    owner: { value: input.owner ?? null, isWritable: false },
    crankProbe: { value: input.crankProbe ?? null, isWritable: true },
    deposit: { value: input.deposit ?? null, isWritable: true },
    magicProgram: { value: input.magicProgram ?? null, isWritable: false },
    magicContext: { value: input.magicContext ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.crankProbe.value) {
    accounts.crankProbe.value = await findCrankProbePda(
      {
        owner: getAddressFromResolvedInstructionAccount(
          "owner",
          accounts.owner.value,
        ),
      },
      { programAddress },
    );
  }
  if (!accounts.magicProgram.value) {
    accounts.magicProgram.value =
      "Magic11111111111111111111111111111111111111" as Address<"Magic11111111111111111111111111111111111111">;
  }
  if (!accounts.magicContext.value) {
    accounts.magicContext.value =
      "MagicContext1111111111111111111111111111111" as Address<"MagicContext1111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("payer", accounts.payer),
      getAccountMeta("owner", accounts.owner),
      getAccountMeta("crankProbe", accounts.crankProbe),
      getAccountMeta("deposit", accounts.deposit),
      getAccountMeta("magicProgram", accounts.magicProgram),
      getAccountMeta("magicContext", accounts.magicContext),
    ],
    data: getCommitAndUndelegateCrankProbeInstructionDataEncoder().encode({}),
    programAddress,
  } as CommitAndUndelegateCrankProbeInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountOwner,
    TAccountCrankProbe,
    TAccountDeposit,
    TAccountMagicProgram,
    TAccountMagicContext
  >);
}

export type CommitAndUndelegateCrankProbeInput<
  TAccountPayer extends string = string,
  TAccountOwner extends string = string,
  TAccountCrankProbe extends string = string,
  TAccountDeposit extends string = string,
  TAccountMagicProgram extends string = string,
  TAccountMagicContext extends string = string,
> = {
  payer: TransactionSigner<TAccountPayer>;
  owner: TransactionSigner<TAccountOwner>;
  crankProbe: Address<TAccountCrankProbe>;
  deposit: Address<TAccountDeposit>;
  magicProgram?: Address<TAccountMagicProgram>;
  magicContext?: Address<TAccountMagicContext>;
};

export function getCommitAndUndelegateCrankProbeInstruction<
  TAccountPayer extends string,
  TAccountOwner extends string,
  TAccountCrankProbe extends string,
  TAccountDeposit extends string,
  TAccountMagicProgram extends string,
  TAccountMagicContext extends string,
  TProgramAddress extends Address = typeof PROTECTED_PAY_PROGRAM_ADDRESS,
>(
  input: CommitAndUndelegateCrankProbeInput<
    TAccountPayer,
    TAccountOwner,
    TAccountCrankProbe,
    TAccountDeposit,
    TAccountMagicProgram,
    TAccountMagicContext
  >,
  config?: { programAddress?: TProgramAddress },
): CommitAndUndelegateCrankProbeInstruction<
  TProgramAddress,
  TAccountPayer,
  TAccountOwner,
  TAccountCrankProbe,
  TAccountDeposit,
  TAccountMagicProgram,
  TAccountMagicContext
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? PROTECTED_PAY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    payer: { value: input.payer ?? null, isWritable: true },
    owner: { value: input.owner ?? null, isWritable: false },
    crankProbe: { value: input.crankProbe ?? null, isWritable: true },
    deposit: { value: input.deposit ?? null, isWritable: true },
    magicProgram: { value: input.magicProgram ?? null, isWritable: false },
    magicContext: { value: input.magicContext ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.magicProgram.value) {
    accounts.magicProgram.value =
      "Magic11111111111111111111111111111111111111" as Address<"Magic11111111111111111111111111111111111111">;
  }
  if (!accounts.magicContext.value) {
    accounts.magicContext.value =
      "MagicContext1111111111111111111111111111111" as Address<"MagicContext1111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("payer", accounts.payer),
      getAccountMeta("owner", accounts.owner),
      getAccountMeta("crankProbe", accounts.crankProbe),
      getAccountMeta("deposit", accounts.deposit),
      getAccountMeta("magicProgram", accounts.magicProgram),
      getAccountMeta("magicContext", accounts.magicContext),
    ],
    data: getCommitAndUndelegateCrankProbeInstructionDataEncoder().encode({}),
    programAddress,
  } as CommitAndUndelegateCrankProbeInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountOwner,
    TAccountCrankProbe,
    TAccountDeposit,
    TAccountMagicProgram,
    TAccountMagicContext
  >);
}

export type ParsedCommitAndUndelegateCrankProbeInstruction<
  TProgram extends string = typeof PROTECTED_PAY_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    payer: TAccountMetas[0];
    owner: TAccountMetas[1];
    crankProbe: TAccountMetas[2];
    deposit: TAccountMetas[3];
    magicProgram: TAccountMetas[4];
    magicContext: TAccountMetas[5];
  };
  data: CommitAndUndelegateCrankProbeInstructionData;
};

export function parseCommitAndUndelegateCrankProbeInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedCommitAndUndelegateCrankProbeInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 6) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 6,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      payer: getNextAccount(),
      owner: getNextAccount(),
      crankProbe: getNextAccount(),
      deposit: getNextAccount(),
      magicProgram: getNextAccount(),
      magicContext: getNextAccount(),
    },
    data: getCommitAndUndelegateCrankProbeInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
