/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyUint8Array,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { PROTECTED_PAY_PROGRAM_ADDRESS } from "../programs";

export const ADVANCE_CRANK_PROBE_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([252, 244, 86, 174, 23, 24, 79, 123]);

export function getAdvanceCrankProbeDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    ADVANCE_CRANK_PROBE_DISCRIMINATOR,
  );
}

export type AdvanceCrankProbeInstruction<
  TProgram extends string = typeof PROTECTED_PAY_PROGRAM_ADDRESS,
  TAccountCrankProbe extends string | AccountMeta<string> = string,
  TAccountDeposit extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountCrankProbe extends string
        ? WritableAccount<TAccountCrankProbe>
        : TAccountCrankProbe,
      TAccountDeposit extends string
        ? WritableAccount<TAccountDeposit>
        : TAccountDeposit,
      ...TRemainingAccounts,
    ]
  >;

export type AdvanceCrankProbeInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type AdvanceCrankProbeInstructionDataArgs = {};

export function getAdvanceCrankProbeInstructionDataEncoder(): FixedSizeEncoder<AdvanceCrankProbeInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: ADVANCE_CRANK_PROBE_DISCRIMINATOR }),
  );
}

export function getAdvanceCrankProbeInstructionDataDecoder(): FixedSizeDecoder<AdvanceCrankProbeInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getAdvanceCrankProbeInstructionDataCodec(): FixedSizeCodec<
  AdvanceCrankProbeInstructionDataArgs,
  AdvanceCrankProbeInstructionData
> {
  return combineCodec(
    getAdvanceCrankProbeInstructionDataEncoder(),
    getAdvanceCrankProbeInstructionDataDecoder(),
  );
}

export type AdvanceCrankProbeInput<
  TAccountCrankProbe extends string = string,
  TAccountDeposit extends string = string,
> = {
  crankProbe: Address<TAccountCrankProbe>;
  deposit: Address<TAccountDeposit>;
};

export function getAdvanceCrankProbeInstruction<
  TAccountCrankProbe extends string,
  TAccountDeposit extends string,
  TProgramAddress extends Address = typeof PROTECTED_PAY_PROGRAM_ADDRESS,
>(
  input: AdvanceCrankProbeInput<TAccountCrankProbe, TAccountDeposit>,
  config?: { programAddress?: TProgramAddress },
): AdvanceCrankProbeInstruction<
  TProgramAddress,
  TAccountCrankProbe,
  TAccountDeposit
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? PROTECTED_PAY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    crankProbe: { value: input.crankProbe ?? null, isWritable: true },
    deposit: { value: input.deposit ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("crankProbe", accounts.crankProbe),
      getAccountMeta("deposit", accounts.deposit),
    ],
    data: getAdvanceCrankProbeInstructionDataEncoder().encode({}),
    programAddress,
  } as AdvanceCrankProbeInstruction<
    TProgramAddress,
    TAccountCrankProbe,
    TAccountDeposit
  >);
}

export type ParsedAdvanceCrankProbeInstruction<
  TProgram extends string = typeof PROTECTED_PAY_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    crankProbe: TAccountMetas[0];
    deposit: TAccountMetas[1];
  };
  data: AdvanceCrankProbeInstructionData;
};

export function parseAdvanceCrankProbeInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedAdvanceCrankProbeInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 2,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { crankProbe: getNextAccount(), deposit: getNextAccount() },
    data: getAdvanceCrankProbeInstructionDataDecoder().decode(instruction.data),
  };
}
